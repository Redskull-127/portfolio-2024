import { signal } from "signals-react-safe";

export const ProjectSignal = signal({} as HTMLButtonElement);
export const ChatSignal = signal({} as HTMLButtonElement);
export const BlogSignal = signal({} as HTMLButtonElement);
